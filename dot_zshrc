# ~/.zshrc

# mise (インタラクティブシェルでのみ有効化)
if command -v mise >/dev/null 2>&1; then
  eval "$(mise activate zsh)"
fi

for f in ~/.config/zsh/*.zsh; do
  [[ -r "$f" ]] && source "$f"
done

# pnpm
export PNPM_HOME="/Users/hiroki.watanabe/Library/pnpm"
case ":$PATH:" in
  *":$PNPM_HOME/bin:"*) ;;
  *) export PATH="$PNPM_HOME/bin:$PATH" ;;
esac
# pnpm end
